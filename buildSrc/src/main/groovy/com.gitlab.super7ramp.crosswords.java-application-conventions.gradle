/**
 * Conventions for Java applications.
 */

plugins {
    id 'application'
    id 'com.gitlab.super7ramp.crosswords.java-conventions'
}

application {
    mainModule = "${project.group}.${project.name}"
    mainClass = "${project.group}.${project.name}.Main"
}