rootProject.name = 'crosswords'

// CLI frontend
include 'cli'

// The application core library and its plugin definitions
include 'crosswords'
include 'crosswords-spi-dictionary'
include 'crosswords-spi-solver'
include 'crosswords-spi-presenter'

// Dictionary plugin implementations/tools/data
include 'dictionary-common'
include 'dictionary-hunspell'
include 'dictionary-internal'
include 'dictionary-tools'
include 'dictionary-data'

// GUI frontend
include 'gui'
include 'gui-controls'

// Solver plugin implementations
include 'solver-ginsberg'

// Util
include 'util'

// Dependency versions
dependencyResolutionManagement {
    versionCatalogs {
        sbom {
            from(files('sbom.toml'))
        }
    }
}